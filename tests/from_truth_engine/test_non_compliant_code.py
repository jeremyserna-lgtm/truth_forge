# This file is intentionally non-compliant to test Builder Service hook
# It's missing: logging, docstring, framework alignment

def bad_function():
    print("This should be blocked")
    return True

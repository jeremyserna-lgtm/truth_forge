Based on the **Genesis Sovereign Execution Plan** and **Genesis Architecture** documents, the **Memory route UI** in the Genesis Console is explicitly designed to provide observability into the **ANIMA** system. It does not just show a list of files; it exposes the active machinery of the **Unified Cognition Bus (UCB)**.  
Specifically, the Memory route UI displays:  
**1\. ANIMA Engine Diagnostics**

* **Active Injection Status:** Real-time diagnostics for the **"five-engine memory"**. This allows operators to see exactly which memory engines are active and what data they are contributing to the current task.  
* **Measurable Request Data:** The UI displays metrics that make memory injection **"measurable in requests,"** allowing verification that the model is receiving the correct context volume and type.

**2\. Injection Content & Traceability**

* **Visible Retrieval:** Operators can see the specific **memory atoms** (code, docs, conversation history) that have been retrieved from the UCB and injected into the model's context stream.  
* **Provenance Graph:** The UI displays the lineage of these injected atoms, allowing you to trace exactly where a piece of memory came from (e.g., a specific previous "Scout" extraction or "Maverick" challenge).

**3\. Unified Cognition State**

* **Shared Context Ledger:** It provides visibility into the **"shared context ledger,"** showing the unified state that is being read and written to by the Triad (Scout, Maverick, R1) during execution.  
* **Cross-Node State:** For distributed operations, it verifies that the "King" and "Soldier" nodes are sharing the same memory contracts and operating as a single cognition substrate.

**4\. Universal Intake Verification**

* **Atomization Status:** It confirms that new inputs (via Universal Intake) have been correctly **"atomized"** and are available for injection, ensuring the "sovereign system of record" is up to date.


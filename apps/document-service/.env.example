# Server Configuration
PORT=3001
NODE_ENV=development

# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=truth-forge
BIGQUERY_PROJECT=truth-forge
BIGQUERY_DATASET=main

# Service Account (for local development)
# Point to your service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=/Users/jeremyserna/.config/gcloud/application_default_credentials.json

# Secrets (stored in Google Secret Manager)
# These will be fetched from Secret Manager instead of storing here
# Secret names: gemini-api-key, openai-api-key, etc.
